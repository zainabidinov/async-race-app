export interface CarTypes {
    name: string;
    color: string;
    id: number;
}

export interface ButtonTypes {
    color: string;
    text: string;
    icon?: string;
    btnType?: string;
    btnSubmitType?: string;
}